import React from "react";
import { Grid, Cell } from "react-md";

import BlockSlider from "..";

const Home = () => (
  <Grid>
    <Cell size={4} />
    <Cell size={4} />
    <Cell size={4} />
  </Grid>
);

export default Home;
